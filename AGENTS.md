# Agent Instructions

- Prefix shell commands with `rtk`.
- Prefer `rg` or `rg --files` for searches.
- Do not revert or overwrite user changes unless explicitly asked.
- Keep changes scoped to the request and follow existing project patterns.
- Run the smallest useful verification before claiming work is complete.
- Push after every commit of changes.

